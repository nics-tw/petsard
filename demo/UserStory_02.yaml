Loader:
  adult:
    filepath: 'benchmark://adult'
    na_values:
      workclass: '?'
      occupation: '?'
      native-country: '?'
Preprocessor:
  demo:
    # for set the method 'default',
    #   Processor will diagnosis the metadata
    #   and set the best pre-processor method for the data
    method: 'default'
Synthesizer:
  sdv-gaussian:
    method: 'sdv-single_table-gaussiancopula'
Postprocessor:
  demo:
    # make sure the expt_name of Postprocessor is same as the Preprocessor
    method: 'default'
Reporter:
  save_data:
    method: 'save_data'
    output: 'UserStory_02'
    # source of Reporter means which result of module/expt_name should Reporter use
    #   accept string (for only one) and list of string (for multiple result)
    source: 'Postprocessor'
